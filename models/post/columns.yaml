# ===================================
#  List Column Definitions
# ===================================

columns:

  id:
    label: teb.afterstory::lang.posts.id
    searchable: false

  category:
    label: teb.afterstory::lang.posts.category_id
    relation: category
    select: name
    searchable: true

  title:
    label: teb.afterstory::lang.posts.title
    searchable: true

  is_best:
    label: teb.afterstory::lang.posts.is_best
    searchable: true

  order_no:
    label: teb.afterstory::lang.posts.order_no
    searchable: false

  user_name:
    label: teb.afterstory::lang.posts.user_name
    relation: user
    select: name
    searchable: true

  created_at:
    label: teb.afterstory::lang.posts.created_at
    searchable: true