# ===================================
#  Form Field Definitions
# ===================================

fields:
    post:
        label: teb.afterstory::lang.photo.title
        type: relation
        select: title

    path:
        label: teb.afterstory::lang.photo.path
