# ===================================
# Filter Scope Definitions
# ===================================

scopes:

    groups:

        # Filter name
        label: teb.afterstory::lang.category.label

        # Model Class name
        modelClass: Teb\Afterstory\Models\Category

        # Model attribute to display for the name
        nameFrom: name

        # Filter scope
        scope: filterByCategory
